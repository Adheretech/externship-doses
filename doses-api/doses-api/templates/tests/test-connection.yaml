apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "doses-api.fullname" . }}-test-connection"
  labels:
    {{- include "doses-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "doses-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
